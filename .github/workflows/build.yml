name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      generator_schema: ${{ steps.generator_schema.outputs.changed }}
      generator_sync: ${{ steps.generator_sync.outputs.changed }}
      prisma_idb_unit: ${{ steps.prisma_idb_unit.outputs.changed }}
      prisma_idb_e2e: ${{ steps.prisma_idb_e2e.outputs.changed }}
      prisma_idb_kanban_e2e: ${{ steps.prisma_idb_kanban_e2e.outputs.changed }}
      generator_client: ${{ steps.generator_client.outputs.changed }}

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - id: generator_schema
        name: Check generator schema test paths
        uses: ./.github/actions/paths-changed
        with:
          paths: |
            packages/generator
            tests/schema-gen
            pnpm-lock.yaml

      - id: generator_client
        name: Check generator client test paths
        uses: ./.github/actions/paths-changed
        with:
          paths: |
            packages/generator
            apps/usage
            pnpm-lock.yaml

      - id: generator_sync
        name: Check generator sync test paths
        uses: ./.github/actions/paths-changed
        with:
          paths: |
            packages/generator
            apps/pidb-kanban-example
            pnpm-lock.yaml

      - id: prisma_idb_unit
        name: Check Prisma 8 unit test paths
        uses: ./.github/actions/paths-changed
        with:
          paths: |
            packages/prisma-orm
            tests/prisma-idb-cli
            pnpm-lock.yaml

      - id: prisma_idb_e2e
        name: Check Prisma 8 E2E test paths
        uses: ./.github/actions/paths-changed
        with:
          paths: |
            packages/prisma-orm
            apps/prisma-orm-usage
            pnpm-lock.yaml

      - id: prisma_idb_kanban_e2e
        name: Check Prisma 8 kanban E2E test paths
        uses: ./.github/actions/paths-changed
        with:
          paths: |
            packages/prisma-orm
            apps/prisma-idb-kanban-example
            pnpm-lock.yaml

  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: ./.github/actions/
      - name: Lint all packages
        run: pnpm lint

  typecheck:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: ./.github/actions/

      - name: Generate BETTER_AUTH_SECRET
        run: echo "BETTER_AUTH_SECRET=$(openssl rand -base64 32 | tr '/+=' '_-')" >> $GITHUB_ENV

      - name: Build Prisma 8 kanban workspace dependencies
        run: pnpm exec turbo run build --filter="...@prisma-idb/prisma-orm-kanban-example^..."

      - name: Emit Prisma 8 kanban Postgres contract
        working-directory: apps/prisma-idb-kanban-example
        run: pnpm contract:emit:postgres
        env:
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5433/postgres"

      - name: Run typecheck commands
        run: pnpm check
        env:
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5433/postgres"
          GOOGLE_CLIENT_ID: "placeholder"
          GOOGLE_CLIENT_SECRET: "placeholder"
          PUBLIC_POSTHOG_PROJECT_TOKEN: "placeholder"
          PUBLIC_POSTHOG_HOST: "https://us.i.posthog.com"

  test-generator-schema:
    needs: changes
    if: needs.changes.outputs.generator_schema == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: ./.github/actions/

      - name: Run schema generation tests
        run: pnpm test:generator-schema

  test-generator-client:
    needs: changes
    if: needs.changes.outputs.generator_client == 'true'
    timeout-minutes: 60
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16@sha256:95206741a5b214807675e14165369d05b93a9cf692223b616d07cca227e74b0b
        env:
          POSTGRES_DB: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5433:5432

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: Set DATABASE_URL environment variable
        run: echo "DATABASE_URL=postgresql://postgres:postgres@localhost:5433/postgres" >> $GITHUB_ENV
      - uses: ./.github/actions/
      - name: Install Playwright Browsers
        working-directory: apps/usage
        run: pnpm exec playwright install --with-deps
      - name: Run generator client Playwright tests
        run: pnpm test:generator-client
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-generator-client
          path: apps/usage/playwright-report/
          retention-days: 30

  test-generator-sync:
    needs: changes
    if: needs.changes.outputs.generator_sync == 'true'
    timeout-minutes: 60
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16@sha256:95206741a5b214807675e14165369d05b93a9cf692223b616d07cca227e74b0b
        env:
          POSTGRES_DB: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5434:5432

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: Set DATABASE_URL environment variable
        run: echo "DATABASE_URL=postgresql://postgres:postgres@localhost:5434/postgres" >> $GITHUB_ENV
      - uses: ./.github/actions/
      - name: Install Playwright Browsers
        working-directory: apps/pidb-kanban-example
        run: pnpm exec playwright install --with-deps
      - name: Generate BETTER_AUTH_SECRET
        run: echo "BETTER_AUTH_SECRET=$(openssl rand -base64 32 | tr '/+=' '_-')" >> $GITHUB_ENV
      - name: Run Sync Playwright tests
        run: pnpm test:generator-sync
        env:
          GOOGLE_CLIENT_ID: "placeholder"
          GOOGLE_CLIENT_SECRET: "placeholder"
          PUBLIC_POSTHOG_PROJECT_TOKEN: "placeholder"
          PUBLIC_POSTHOG_HOST: "https://us.i.posthog.com"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-generator-sync
          path: apps/pidb-kanban-example/playwright-report/
          retention-days: 30

  test-prisma-idb:
    needs: changes
    if: needs.changes.outputs.prisma_idb_unit == 'true'
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16@sha256:95206741a5b214807675e14165369d05b93a9cf692223b616d07cca227e74b0b
        env:
          POSTGRES_DB: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5436:5432

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
      - name: Set DATABASE_URL environment variable
        run: echo "DATABASE_URL=postgresql://postgres:postgres@localhost:5436/postgres" >> $GITHUB_ENV
      - uses: ./.github/actions/

      - name: Run Prisma 8 unit tests
        run: pnpm test:prisma-idb

  test-prisma-idb-e2e:
    needs: changes
    if: needs.changes.outputs.prisma_idb_e2e == 'true'
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: ./.github/actions/

      - name: Install Playwright Browsers
        working-directory: apps/prisma-orm-usage
        run: pnpm exec playwright install --with-deps

      - name: Run Prisma 8 E2E tests
        run: pnpm test:prisma-idb-e2e

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-prisma-idb
          path: apps/prisma-orm-usage/playwright-report/
          retention-days: 30

  test-prisma-idb-kanban-e2e:
    needs: changes
    if: needs.changes.outputs.prisma_idb_kanban_e2e == 'true'
    timeout-minutes: 60
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16@sha256:95206741a5b214807675e14165369d05b93a9cf692223b616d07cca227e74b0b
        env:
          POSTGRES_DB: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5435:5432

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Set DATABASE_URL environment variable
        run: echo "DATABASE_URL=postgresql://postgres:postgres@localhost:5435/postgres" >> $GITHUB_ENV
      - uses: ./.github/actions/
      - name: Generate BETTER_AUTH_SECRET
        run: echo "BETTER_AUTH_SECRET=$(openssl rand -base64 32 | tr '/+=' '_-')" >> $GITHUB_ENV

      - name: Build Prisma 8 kanban workspace dependencies
        run: pnpm exec turbo run build --filter="...@prisma-idb/prisma-orm-kanban-example^..."

      - name: Emit Postgres contract
        working-directory: apps/prisma-idb-kanban-example
        run: pnpm contract:emit:postgres

      - name: Initialize Postgres schema
        working-directory: apps/prisma-idb-kanban-example
        run: pnpm db:init

      - name: Install Playwright Browsers
        working-directory: apps/prisma-idb-kanban-example
        run: pnpm exec playwright install --with-deps

      - name: Run Prisma 8 kanban E2E tests
        run: pnpm test:prisma-idb-kanban-e2e
        env:
          GOOGLE_CLIENT_ID: "placeholder"
          GOOGLE_CLIENT_SECRET: "placeholder"

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-prisma-idb-kanban
          path: apps/prisma-idb-kanban-example/playwright-report/
          retention-days: 30

  ci-required:
    name: CI required
    needs:
      - changes
      - lint
      - typecheck
      - test-generator-schema
      - test-generator-client
      - test-generator-sync
      - test-prisma-idb
      - test-prisma-idb-e2e
      - test-prisma-idb-kanban-e2e
    if: ${{ always() }}
    runs-on: ubuntu-latest

    steps:
      - name: Check required CI results
        env:
          CHANGES_RESULT: ${{ needs.changes.result }}
          LINT_RESULT: ${{ needs.lint.result }}
          TYPECHECK_RESULT: ${{ needs.typecheck.result }}
          GENERATOR_SCHEMA_REQUIRED: ${{ needs.changes.outputs.generator_schema }}
          GENERATOR_SCHEMA_RESULT: ${{ needs['test-generator-schema'].result }}
          GENERATOR_CLIENT_REQUIRED: ${{ needs.changes.outputs.generator_client }}
          GENERATOR_CLIENT_RESULT: ${{ needs['test-generator-client'].result }}
          GENERATOR_SYNC_REQUIRED: ${{ needs.changes.outputs.generator_sync }}
          GENERATOR_SYNC_RESULT: ${{ needs['test-generator-sync'].result }}
          PRISMA_IDB_UNIT_REQUIRED: ${{ needs.changes.outputs.prisma_idb_unit }}
          PRISMA_IDB_UNIT_RESULT: ${{ needs['test-prisma-idb'].result }}
          PRISMA_IDB_E2E_REQUIRED: ${{ needs.changes.outputs.prisma_idb_e2e }}
          PRISMA_IDB_E2E_RESULT: ${{ needs['test-prisma-idb-e2e'].result }}
          PRISMA_IDB_KANBAN_E2E_REQUIRED: ${{ needs.changes.outputs.prisma_idb_kanban_e2e }}
          PRISMA_IDB_KANBAN_E2E_RESULT: ${{ needs['test-prisma-idb-kanban-e2e'].result }}
        run: |
          set -euo pipefail

          require_success() {
            local name="$1"
            local result="$2"

            if [ "$result" != "success" ]; then
              echo "$name ended with: $result"
              exit 1
            fi
          }

          require_conditional_success() {
            local name="$1"
            local required="$2"
            local result="$3"

            if [ "$required" = "true" ]; then
              require_success "$name" "$result"
            else
              echo "$name was not required for these paths."
            fi
          }

          require_success "Path detection" "$CHANGES_RESULT"
          require_success "Lint" "$LINT_RESULT"
          require_success "Typecheck" "$TYPECHECK_RESULT"
          require_conditional_success "Generator schema tests" "$GENERATOR_SCHEMA_REQUIRED" "$GENERATOR_SCHEMA_RESULT"
          require_conditional_success "Generator client tests" "$GENERATOR_CLIENT_REQUIRED" "$GENERATOR_CLIENT_RESULT"
          require_conditional_success "Generator sync tests" "$GENERATOR_SYNC_REQUIRED" "$GENERATOR_SYNC_RESULT"
          require_conditional_success "Prisma 8 unit tests" "$PRISMA_IDB_UNIT_REQUIRED" "$PRISMA_IDB_UNIT_RESULT"
          require_conditional_success "Prisma 8 E2E tests" "$PRISMA_IDB_E2E_REQUIRED" "$PRISMA_IDB_E2E_RESULT"
          require_conditional_success "Prisma 8 kanban E2E tests" "$PRISMA_IDB_KANBAN_E2E_REQUIRED" "$PRISMA_IDB_KANBAN_E2E_RESULT"

          echo "Required CI checks are satisfied."
