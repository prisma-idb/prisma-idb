# Regenerated every prisma.config.ts load — see its header.
test/fixtures/schema.generated.prisma
test/fixtures/schema.generated.json
test/fixtures/schema.generated.d.ts

# Bootstrap-only (`db init`, never authored migrations) — fully derived
# per-environment state, not portable history.
test/fixtures/migrations-postgres/
