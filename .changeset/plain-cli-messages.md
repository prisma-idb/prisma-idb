---
"@prisma-idb/family-idb": patch
---

`contract emit` warnings and errors about `@idb.exclude`, and the `migration plan --space` help text, no longer point at design documents. They now say what happened and what to do in the message itself.
